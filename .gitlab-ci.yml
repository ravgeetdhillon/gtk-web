image: ruby:2.5

variables:
  JEKYLL_ENV: production
  LC_ALL: C.UTF-8

stages:
  - installation
  - structuring
  - generation
  - test
  - deploy

api_data:
  stage: installation
  script:
  - mkdir ./_data/api_fetch
  - curl -o- https://gitlab.gnome.org/api/v4/projects/665/repository/tags?order_by=name > ./_data/api_fetch/tags.json
  - curl -o- https://api.rss2json.com/v1/api.json?rss_url=https%3A%2F%2Fblog.gtk.org%2Ffeed%2F > ./_data/api_fetch/gtk-blog-feed.json
  - curl -o- https://gitlab.gnome.org/api/v4/projects/665/issues_statistics > ./_data/api_fetch/issues_stats.json
  - curl -o- https://gitlab.gnome.org/api/v4/projects/665/issues?state=opened > ./_data/api_fetch/tags.json
  artifacts:
    paths:
    - _data/api_fetch

npm:
  stage: installation
  script:
  - apt-get update -y
  - apt-get install -y curl
  - curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.0/install.sh | bash
  - source /root/.bashrc
  - nvm install node
  - nvm use node
  - npm install
  artifacts:
    paths:
    - node_modules

structure:
  stage: structuring
  script:
  - mv ./node_modules/* ./assets/ -f
  - rm -rf ../node_modules

sass:
  stage: generation
  script: 
  - npm run sass

test:
  stage: test
  script:
  - gem install bundler jekyll
  - bundle install
  - bundle exec jekyll build -d test
  artifacts:
    paths:
    - test
  except:
  - master

pages:
  stage: deploy
  script:
  - gem install bundler jekyll
  - bundle install
  - bundle exec jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master
